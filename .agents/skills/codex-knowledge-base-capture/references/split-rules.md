# Split Rules

Use these rules:

- `Concepts/` for definitions and decision rules
- `Workflows/` for end-to-end process
- `Skills/` for reusable operating contracts
- `Agents/` for role boundaries and handoffs
- `Samples/` for one real business case

If the user says "I want this callable later", prefer creating a skill package in addition to notes.

