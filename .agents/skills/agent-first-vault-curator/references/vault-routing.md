# Vault Routing

Route extracted material by reuse intent:

- `Concepts/` for stable ideas and decision rules
- `Workflows/` for end-to-end procedures
- `Skills/` for reusable operating instructions
- `Agents/` for role boundaries and handoffs
- `Sources/` for lineage and refreshability
- `Samples/` for concrete examples

Do not collapse everything into one overview page. The overview and MOC should link out, not hold all detail.
