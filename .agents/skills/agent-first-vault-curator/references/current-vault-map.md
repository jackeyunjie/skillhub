# Current Vault Map

This repository already contains:

- `Concepts/` for Agent First, automation, plugin boundaries, and repeatable work
- `Workflows/` for daily intel, content factory, video editing, query webapp, and knowledge base capture
- `Skills/` for workflow builder, HTML brief generation, automation task spec, and query webapp builder
- `Agents/` for research, briefing, content, and automation roles
- `Samples/` for concrete case pages
- `Sources/` for source lineage

Use this map to avoid duplicating an existing note when an update or fold-in is more appropriate.
