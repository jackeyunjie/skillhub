# Output Checks

Check:

- edited file exists
- subtitle file exists
- subtitle structure is valid
- obvious transcription errors are not ignored

If quality is uncertain, say the output still needs manual review in an editor.

